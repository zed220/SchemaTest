<Application x:Class="SchemeTester.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:SchemeTester.Controls"
             xmlns:converters="clr-namespace:SchemeTester.Converters"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <Style TargetType="{x:Type controls:SchemeControl}">
            <Setter Property="FillColor" Value="#3300FF00"/>
            <Setter Property="StrokeColor" Value="#55000000"/>
            <Setter Property="DotColor" Value="Black"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type controls:SchemeControl}">
                        <Grid>
                            <Path x:Name="pathFill" Data="{TemplateBinding PathFill}" Stretch="None" Stroke="White" Fill="{TemplateBinding FillColor, Converter={converters:ColorToBrushConverter}}" />
                            <Path x:Name="pathScheme" Data="{TemplateBinding PathScheme}" Stretch="None" Stroke="{TemplateBinding StrokeColor, Converter={converters:ColorToBrushConverter}}" Fill="{TemplateBinding DotColor, Converter={converters:ColorToBrushConverter}}" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
